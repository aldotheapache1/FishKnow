<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.FishKnow.arcoremeasure">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ListaMedir"/>
        <activity android:name=".MedidaNegada" />
        <activity android:name=".MedidaPermitida" />
        <activity android:name=".DetalhePeixeAlt" />
        <activity android:name=".DetalhePeixe" />
        <activity android:name=".DetalheEquipamento" />
        <activity android:name=".ArMeasureActivityAlt" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name"
            android:screenOrientation="locked"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListaEquipamento" />
        <activity android:name=".ArMeasureActivity" />
        <activity android:name=".ListaPeixes" />

        <meta-data
            android:name="com.google.ar.core"
            android:value="required" />
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="83b15a229c32b57e4bc08478aafddff574a695ab" />

    </application>

</manifest>